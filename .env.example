# n8n Webhook URL for contact form submissions (server-side only, never exposed to client)
# This webhook receives POST requests with: { name, email, company, role, need, notes, timestamp, source }
N8N_CONTACT_WEBHOOK_URL=https://your-n8n-instance.com/webhook/your-webhook-id
